---
layout: layouts/base.njk
---

<script type="application/ld+json">
{
	"@context": "https://schema.org",
	"@type": "BlogPosting",
	"headline": {{ title | dump }},
	"datePublished": "{{ date | htmlDateString }}",
	"dateModified": "{{ date | htmlDateString }}",
	"description": {{ (description or "") | dump }},
	"url": "{{ metadata.url }}{{ page.url }}",
	"author": {
		"@type": "Person",
		"name": {{ metadata.author.name | dump }},
		"url": {{ metadata.author.url | dump }}
	}
}
</script>

{# Only include the syntax highlighter and KaTeX CSS on blog posts, included with the CSS per-page bundle. #}
{# Capture the CSS content as a Nunjucks variable and feed it through to the cssmin filter to minify it. #}
{% set css %}
{% include "styles/prism-github.css" %}
{% include "styles/katex.css" %}
{% include "styles/blog-post.css" %}
{% endset %}
<style>
	{{css | cssmin | safe}}
</style>

<article>
	<header class="blog-post__header">
		<h1 class="common-main-title">{{ title }}</h1>
		<div class="blog-post__metadata">
			Published on
			<time datetime="{{ date | htmlDateString }}">
				{{ date | readableDate("d LLLL yyyy") }}
			</time>
			in
			<span>
				{{ tags | readableTags | join(", ") }}
			</span>
		</div>
	</header>

	<div class="common-main-content">
		{{ content | safe }}
	</div>
</article>
